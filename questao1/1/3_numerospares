# Programa que imprime todos os números pares de 0 até um número informado


limite = int(input("Digite um número inteiro: "))

print(f"Números pares de 0 até {limite}:")

for i in range(0, limite + 1):
    if i % 2 == 0:
        print(i)
